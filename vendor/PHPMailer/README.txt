PHPMailer 7.1.1 (LGPL-2.1), vendored — only the three files send.php needs.
Upstream: https://github.com/PHPMailer/PHPMailer/releases/tag/v7.1.1
Vendored rather than Composer-installed because shared Greek hosts often lack CLI access.
To update: replace src/*.php from a new tag and re-test the form.
